# Base de datos MySQL
DB_USER=root
DB_PASSWORD=tu_password
DB_HOST=localhost
DB_PORT=3306
DB_NAME=ticketapp

# JWT — cambiá esto por una clave larga y aleatoria en producción
JWT_SECRET=cambia_esto_por_una_clave_secreta
JWT_EXPIRATION_HOURS=24

# Servidor
PORT=8080
